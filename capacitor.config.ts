import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Kampung_Siber',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
